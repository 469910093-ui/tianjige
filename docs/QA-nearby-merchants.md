# 附近商家真实性 QA 报告

**日期**：2026-07-12  
**结论**：FAIL / NEEDS WORK —— **当前展示的推荐不是真实 POI**

## 证据

| 项 | 结果 |
|---|---|
| `BAIDU_MAP_AK` / `TENCENT_MAP_KEY` | 均为空 |
| `node_modules` | 不存在，Next `/api/nearby` 未运行 |
| 用户实际打开的预览 | `http://127.0.0.1:8765/ui-mockup.html` |
| 预览数据源 | 曾硬编码「半亩方塘·本帮」等假店名 |
| 用户在美团/地图搜不到 | **符合预期**（假数据） |

## 门禁（须全过才放行）

- [ ] 配置有效 `BAIDU_MAP_AK`（或腾讯 Key）
- [ ] `pnpm install && pnpm dev` 跑通
- [ ] 前端请求 `/api/nearby`，返回 `ok:true` + `provider`
- [ ] 卡片显示来源「百度地图/腾讯地图」
- [ ] 抽样 ≥3 家能在百度地图/美团搜到同名同址
- [ ] Key 缺失时显式报错，禁止静默假数据

## 已做质检修复

- HTML 预览已清空假商家列表，并加红色 **QA FAIL** 提示，避免继续误导。

## 放行前你需要提供

在 `.env.local` 填入真实 `BAIDU_MAP_AK=` 后告诉我，我再帮你启动正式服务并做一轮真机抽样核验。
